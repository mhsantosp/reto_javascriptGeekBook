const input = document.querySelector("input");

console.dir(input);

/* value: "" => Devuelve el valor que se le dio a la etiqueta.
  nodeName: "INPUT" => Devuelve el nombre del nodo en especifico.
  required: false => Devuelve si el input fue un campo obligatorio o no.
  
estamos trabajando sobre un objeto es una propiedad*/
